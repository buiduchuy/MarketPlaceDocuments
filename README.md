Node version: v24.14.0
HardHat version: v2.28.6
etherjs version: ethers@6.16.0
How to run this sh*t:
1. Open the first terminal then use this command: npx hardhat node
there will be about 20 accounts and you must import any of that into Metamask ( remember to connect the correct Network ChainID 31337, url: "http://127.0.0.1:8545" )
2. Run npx hardhat run scripts/deploy.js --network localhost, copy 3 that IBA into contract.js
3. go to frontend path by "cd frontend" and run "npm start" to compile and display it to brownche
4. Good Luck !!!
